package datastructures.stackandqueue;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class StackTest {
  @Test
  voind instantiateAStack(){
    Stack stack = new Stack();
    System.out.println(stack);
    assertTrue(true);
  }
}
